### Bandit config file generated by Anthropic Claude

# Skip specific error codes
skips: ['B101']  # Skips assert warning

# Optional: Configure additional settings
exclude_dirs: [
    'tests',
    'venv',
    '.git',
    '__pycache__'
]

# Optional: Set severity levels
any_other_function_with_shell_equals_true: HIGH
hardcoded_tmp_directory: MEDIUM
